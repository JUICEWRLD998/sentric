[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
solc = "0.8.30"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200

# Sentric — autonomous on-chain portfolio-insurance agent for Somnia Event Contracts.
# See ../implementation.md for the full spec.
